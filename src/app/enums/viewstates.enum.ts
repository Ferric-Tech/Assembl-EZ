export enum QuotePageViewState {
  PRODUCT_SELECT = 1,
  PRODUCT_MEASUREMENTS = 2,
  QUOTE_PARAMETERS = 3,
  RESULTS = 4,
}

export enum ProductsPageViewState {
  MENU = 1,
  PRODUCTS = 2,
  COMPONENTS = 3,
}
